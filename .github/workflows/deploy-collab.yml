name: Build and Deploy Collab Platform

# Independent pipeline for the collab-platform app. Only runs when its own
# files change, so it never deploys alongside AI Tracker.
on:
  push:
    branches:
      - main
    paths:
      - "collab-platform/**"
      - ".github/workflows/deploy-collab.yml"
  workflow_dispatch:

env:
  ACR_NAME: aitrackeracr # shared registry, separate image
  IMAGE_NAME: collab
  RESOURCE_GROUP: rg-aitracker
  CONTAINER_APP: collab-app

# Only secret needed: AZURE_CREDENTIALS (existing service principal JSON).
# DATABASE_URL / AUTH_SECRET are set once on the Container App by
# collab-platform/scripts/provision-azure.sh and persist across deploys.

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build and push image
        run: |
          az acr build \
            --registry ${{ env.ACR_NAME }} \
            --image ${{ env.IMAGE_NAME }}:${{ github.sha }} \
            --image ${{ env.IMAGE_NAME }}:latest \
            --file collab-platform/Dockerfile \
            collab-platform

      - name: Deploy to Container App
        run: |
          az containerapp update \
            --name ${{ env.CONTAINER_APP }} \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --image ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
